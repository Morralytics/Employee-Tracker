const inquirer = require('inquirer');
const fs = require('fs');

const questions = async () {
    const answers = await inquirer
        .prompt([
            {
                
            }
        ])
}